<div class="{{styles.form}}">
  <nav class="{{styles.menu}}">
    <div class="{{styles.chat-add-button}}">
      {{{buttonCreate}}}
    </div>

    <ul class="{{styles.menu-list}}">
      <li>
        <div class="{{styles.chat-app-title-container}}">
          <div class="{{styles.chat-app-title}}">
            MessageApp
          </div>
          <div class="{{styles.dropdown}}">
            {{{buttonMore}}}
            <div name="mainDropdown" class="{{styles.dropdown-content}}">
              <p name="toProfile">Профиль</p>
              <p name="createChats">Создать новый чат</p>
            </div>
          </div>
        </div>
      </li>
      <li>
        <div class="{{styles.separator}}"><div
            class="{{styles.separator-line}}"
          ></div></div>
      </li>
      {{{chatsList}}}
    </ul>
  </nav>

  <div class="{{styles.chat-message-container}}">
    {{{messageContainer}}}
  </div>

  {{{usersListModal}}}
  {{{chatCreateModal}}}

</div>
