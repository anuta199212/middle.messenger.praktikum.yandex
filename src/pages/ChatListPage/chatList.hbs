<form class="{{styles.form}}">
  <nav class="{{styles.menu}}">
    <ul class="{{styles.menu-list}}">
      <li>
        <div class="{{chatListStyles.chat-app-title-container}}">
          <div class="{{chatListStyles.chat-app-title}}">
            MessageApp
          </div>
          <button class="{{chatListStyles.chat-btn-more}}">
            <i
              class="fa-solid fa-ellipsis-vertical {{chatListStyles.chat-btn-more-color}}"
            ></i>
          </button>
        </div>
      </li>
      <li>
        <div class="{{chatListStyles.chat-search-container}}">
          <input placeholder="Поиск" class="{{chatListStyles.chat-search-input}}" />
        </div>
      </li>
      {{#each chatList.item}}
        <li class="{{../styles.menu-item}}">
          <a href="#">
            <table class="{{../chatListStyles.width-100}}">
              <tr>
                <td
                  rowspan="2"
                  class="{{../styles.text-align-center}} {{../chatListStyles.width-45}}"
                >
                  <img
                    src="{{avatar}}"
                    alt="Avatar"
                    class="{{../chatListStyles.chat-avatar-img}}"
                  />
                </td>
                <td class="{{../chatListStyles.chat-title}}">
                  {{name}}
                </td>
                <td
                  class="{{../chatListStyles.chat-message-text}}
                    {{../styles.text-align-right}}
                    {{../chatListStyles.width-35}}"
                >
                  {{lastMessage.time}}
                </td>
              </tr>
              <tr>
                <td
                  class="{{../chatListStyles.chat-message-text}}
                    {{../styles.text-align-left}}"
                >
                  {{lastMessage.text}}
                </td>
                <td>
                  {{#if unReadCount}}
                    <span class="{{../chatListStyles.chat-badge}}">{{unReadCount}}</span>
                  {{/if}}
                </td>
              </tr>
            </table>
          </a>
        </li>
      {{/each}}
    </ul>
  </nav>

  <div class="{{chatListStyles.chat-message-container}}">
    {{#each messageList.message}}
      <div class="{{../chatListStyles.chat-message}} {{class}}">
        <p>{{text}}</p>
        <!--<div><img src="{{image}}" alt="Image"></div>-->
        <div
          class="{{../chatListStyles.chat-message-text}} {{../chatListStyles.chat-message-time}}"
        >{{time}}</div>
      </div>
    {{/each}}
  </div>

  <div class="{{chatListStyles.chat-message-send-container}}">
    <input
      placeholder="Сообщение"
      class="{{chatListStyles.chat-message-send}}"
      name="message"
    />
    <button class="{{chatListStyles.chat-btn}}">
      <i class="fa-solid fa-arrow-right"></i>
    </button>
  </div>

</form>
