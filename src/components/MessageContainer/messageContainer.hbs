{{#if activeChat}}
  <div class="{{styles.message-container-header}}">{{{header}}}</div>

{{else}}
  <div class="{{styles.empty-chat}}"><p class="{{styles.empty-chat-p}}">Выберите
      чат</p></div>
{{/if}}
